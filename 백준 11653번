// 백준 11653번 소인수분해

#include <iostream>
#include <cassert>
using namespace std;

int main()
{
	int N;
	
	cin >> N;
	assert(N >= 1 && N <= 10000000);

	for (int i = 2; i <= N; i++)
	{
		if (N == 1)
		{
			break;
		}

		while (N % i == 0)
		{
			N = N / i;
			cout << i << endl;
		}
	}
}
